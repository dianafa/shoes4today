<h1>Listing pants_n_shirts</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pants_n_shirts.each do |pants_n_shirt| %>
      <tr>
        <td><%= link_to 'Show', pants_n_shirt %></td>
        <td><%= link_to 'Edit', edit_pants_n_shirt_path(pants_n_shirt) %></td>
        <td><%= link_to 'Destroy', pants_n_shirt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pants n shirt', new_pants_n_shirt_path %>
