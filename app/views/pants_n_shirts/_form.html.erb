<%= form_for(@pants_n_shirt) do |f| %>
  <% if @pants_n_shirt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pants_n_shirt.errors.count, "error") %> prohibited this pants_n_shirt from being saved:</h2>

      <ul>
      <% @pants_n_shirt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
